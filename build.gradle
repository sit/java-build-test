description = "A simple project for testing whether incremental builds work properly."
group = 'net.emilsit'
version = '1.0.0'

apply plugin: 'java'
sourceCompatibility = 1.6
targetCompatibility = 1.6

repositories {
    mavenLocal()
}

dependencies {
    testCompile 'junit:junit:4.8.2'
}
